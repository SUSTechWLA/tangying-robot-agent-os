name: ci

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

# Feature commits run through their PR, main and release tags through push.
# Starting both events for the same PR head left cancelled duplicate checks
# (including a failed always-run gate) attached to the very same commit.
# Newer commits still supersede obsolete checks for the same branch.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.head.repo.full_name || github.repository }}-${{ github.event.pull_request.head.ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04
    # The full suite runs the Go, Python and Web tests plus the e2e stacks,
    # which take roughly 2.5x longer here than on a developer machine
    # (software rendering, two cores). 20 minutes killed the job before it
    # could report anything.
    timeout-minutes: 45
    env:
      MUJOCO_GL: 'osmesa'
      PYTHONUNBUFFERED: '1'
      PYTEST_ADDOPTS: '-vv'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26.x'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: sudo apt-get update && sudo apt-get install -y protobuf-compiler libosmesa6
      - name: Install pinned protobuf generators
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.11
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@v1.6.0
          curl --fail --location --retry 5 --retry-delay 10 --connect-timeout 30 --max-time 180 -o "$RUNNER_TEMP/protoc.zip" https://github.com/protocolbuffers/protobuf/releases/download/v34.1/protoc-34.1-linux-x86_64.zip
          echo "af27ea66cd26938fe48587804ca7d4817457a08350021a1c6e23a27ccc8c6904  $RUNNER_TEMP/protoc.zip" | sha256sum --check
          unzip -q "$RUNNER_TEMP/protoc.zip" -d "$RUNNER_TEMP/protoc"
          echo "$RUNNER_TEMP/protoc/bin" >> "$GITHUB_PATH"
      - run: make setup
      - run: .venv/bin/pip install -e '.[visual]'
      - run: echo "RUNTIME_CLIENT_PROBE=$RUNNER_TEMP/runtime-client-probe" >> "$GITHUB_ENV"
      - run: go build -o "$RUNTIME_CLIENT_PROBE" ./tests/contract/runtime_client_probe
      - run: PYTHONUNBUFFERED=1 timeout 5m .venv/bin/pytest --collect-only -q
      - run: make test
      - run: make lint
      - run: make generate-check
      - run: go test ./tests/docs
      - run: make build
      - run: bash scripts/demo.sh
      - run: .venv/bin/python scripts/run_simulation_acceptance.py --episodes 30 --seed 20260817

  installer-dry-run:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            role: sim
            test_os: linux
            distro: ubuntu
            version: '24.04'
            arch: amd64
          - runner: ubuntu-24.04
            role: local
            test_os: linux
            distro: ubuntu
            version: '22.04'
            arch: amd64
          - runner: ubuntu-24.04
            role: robot-pi
            test_os: linux
            distro: ubuntu
            version: '24.04'
            arch: arm64
          - runner: macos-14
            role: sim
            test_os: darwin
            distro: macos
            version: '14'
            arch: arm64
          - runner: macos-14
            role: local
            test_os: darwin
            distro: macos
            version: '14'
            arch: arm64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - name: Validate ${{ matrix.role }} install plan
        env:
          ROBOT_AGENT_TEST_MODE: '1'
          ROBOT_AGENT_TEST_OS: ${{ matrix.test_os }}
          ROBOT_AGENT_TEST_DISTRO: ${{ matrix.distro }}
          ROBOT_AGENT_TEST_VERSION: ${{ matrix.version }}
          ROBOT_AGENT_TEST_ARCH: ${{ matrix.arch }}
        run: bash install.sh "${{ matrix.role }}" --dry-run --yes

  robot-pi-dependencies:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Resolve the actual Robot Edge dependencies on Linux ARM64
        run: |
          python -m venv "$RUNNER_TEMP/robot-pi-venv"
          "$RUNNER_TEMP/robot-pi-venv/bin/python" -m pip install --dry-run --ignore-installed \
            --report "$RUNNER_TEMP/robot-pi-dependencies.json" -e '.[robot-pi]'
      - uses: actions/upload-artifact@v4
        with:
          name: robot-pi-dependency-resolution
          path: ${{ runner.temp }}/robot-pi-dependencies.json

  ros-build:
    runs-on: ubuntu-24.04
    container: ros:jazzy-ros-base
    steps:
      - uses: actions/checkout@v4
      - run: apt-get update && apt-get install -y python3-venv && rosdep update && rosdep install --from-paths robot/ros2_ws/src --ignore-src --skip-keys ament_python -r -y
      - name: Install shared protocol dependencies for ROS tests
        run: |
          python3 -m venv --system-site-packages "$RUNNER_TEMP/ros-protocol-venv"
          "$RUNNER_TEMP/ros-protocol-venv/bin/python" - <<'PY'
          import os
          import subprocess
          import sys
          import sysconfig
          import tomllib
          from pathlib import Path

          dependencies = tomllib.loads(Path("pyproject.toml").read_text())["project"]["dependencies"]
          pins = [next(dep for dep in dependencies if dep.startswith(name + "=="))
                  for name in ("grpcio", "protobuf")]
          subprocess.run([sys.executable, "-m", "pip", "install", *pins], check=True)
          with open(os.environ["GITHUB_ENV"], "a") as environment:
              environment.write(f"ROS_PROTOCOL_SITE_PACKAGES={sysconfig.get_path('purelib')}\n")
          PY
      - run: cd robot/ros2_ws && . /opt/ros/jazzy/setup.sh && colcon build --event-handlers console_direct+
      - name: Test continuous goal scoring and installed DWB plugin loading
        working-directory: robot/ros2_ws
        run: |
          . /opt/ros/jazzy/setup.sh
          . install/setup.sh
          export PYTHONPATH="$ROS_PROTOCOL_SITE_PACKAGES:$GITHUB_WORKSPACE/python${PYTHONPATH:+:$PYTHONPATH}"
          python3 -c 'import rclpy; from tangying_robot_proto.robot.v1 import robot_pb2, robot_pb2_grpc; print("ROS and shared protocol imports OK")'
          colcon test --packages-select tangying_navigation tangying_dwb_critics --return-code-on-test-failure --event-handlers console_direct+
      - name: Report ROS test results
        if: ${{ always() }}
        working-directory: robot/ros2_ws
        run: |
          . /opt/ros/jazzy/setup.sh
          colcon test-result --verbose

  mujoco-compat:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      MUJOCO_GL: 'osmesa'
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: sudo apt-get update && sudo apt-get install -y libosmesa6
      - run: python -m venv .venv && .venv/bin/pip install -e '.[dev,visual]'
      - name: Exercise the next MuJoCo without changing the release baseline
        run: |
          .venv/bin/pip install --no-deps --target "$RUNNER_TEMP/mujoco-compat" mujoco==3.12.0
          PYTHONPATH="$RUNNER_TEMP/mujoco-compat" .venv/bin/python -m pytest -q \
            sim/mujoco/tests/test_self_filter.py \
            sim/mujoco/tests/test_rgbd_workcell.py

  gazebo-contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m venv .venv && .venv/bin/pip install -e '.[dev]'
      # Gazebo is the default engine, and `mujoco-compat` above proves the
      # non-default one still works. Nothing proved the default one, so the scene
      # generator and the workcell detector could drift apart here and only
      # surface as "the arm arrived and the object vanished" during an acceptance
      # run. These tests are the deployment-independent half of that contract:
      # no Docker, no simulator, no rendered pixels.
      - name: Scene generator and workcell detector must describe one workcell
        run: .venv/bin/python -m pytest -q tests/install/test_gazebo_default.py tests/install/test_gazebo_workcell_contract.py
      # The acceptance verifier is a delivered tool, so its own contract is
      # checked here rather than only when somebody runs it against a stack.
      - name: Gazebo acceptance tooling loads and reports its contract
        run: |
          .venv/bin/python scripts/gazebo_contract_check.py --help > /dev/null
          .venv/bin/python scripts/gazebo_probe.py --help > /dev/null
          .venv/bin/python scripts/reset_runtime_latch.py --help > /dev/null
      - name: Gazebo stack and image build scripts parse
        run: bash -n scripts/sim-stack.sh scripts/gazebo-house-stack.sh scripts/navigation-stack.sh

  book-publication:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Build and check the authoritative book sources
        run: |
          python -m venv .venv-book
          .venv-book/bin/pip install -r book/requirements.txt pytest==8.4.2 pytest-timeout==2.4.0
          make book-check
          make book-release
          .venv-book/bin/python -m pytest -q -o addopts='' tests/book
      - name: Validate EPUB with the pinned W3C checker
        run: |
          curl --fail --location --retry 3 --connect-timeout 30 --max-time 240 \
            -o "$RUNNER_TEMP/epubcheck.zip" \
            https://github.com/w3c/epubcheck/releases/download/v5.4.0/epubcheck-5.4.0.zip
          echo "33350c61038e71dfb3d45a76aed04bf5481e6d5500cb780f6e98db8bbd15a28c  $RUNNER_TEMP/epubcheck.zip" | sha256sum --check
          unzip -q "$RUNNER_TEMP/epubcheck.zip" -d "$RUNNER_TEMP/epubcheck"
          java -jar "$RUNNER_TEMP/epubcheck/epubcheck-5.4.0/epubcheck.jar" artifacts/book/1.0.0/book.epub
      - uses: actions/upload-artifact@v4
        with:
          name: robot-agentos-book-1.0.0
          path: artifacts/book/1.0.0/
          if-no-files-found: error

  release-gate:
    if: ${{ always() }}
    needs: [test, installer-dry-run, robot-pi-dependencies, ros-build, mujoco-compat, gazebo-contract, book-publication]
    runs-on: ubuntu-24.04
    steps:
      - name: Require every validation job to succeed
        env:
          VALIDATION_RESULTS: ${{ toJSON(needs) }}
        run: |
          python3 - <<'PYTHON'
          import json
          import os
          results = json.loads(os.environ["VALIDATION_RESULTS"])
          failed = [name for name, job in results.items() if job["result"] != "success"]
          if failed:
              raise SystemExit("Release gate rejected: " + ", ".join(failed))
          print("All required validation jobs passed.")
          PYTHON
