{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:tangying:decision-payloads:v1",
  "title": "决策上下文完整生产者字段契约（分别使用 $defs 验收）",
  "$defs": {
    "goal": {
      "type": "object",
      "description": "用户目标解析器；原始用户要求是权威，模型提取须保留来源。",
      "properties": {
        "request": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 request；无法建立时显式 null，不推断默认值。"
        },
        "target_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 target_id；无法建立时显式 null，不推断默认值。"
        },
        "destination_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 destination_id；无法建立时显式 null，不推断默认值。"
        },
        "forbidden_ids": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 forbidden_ids；无法建立时显式 null，不推断默认值。"
        },
        "clarification_required": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "生产者声明的 clarification_required；无法建立时显式 null，不推断默认值。"
        },
        "current_step_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 current_step_id；无法建立时显式 null，不推断默认值。"
        },
        "object_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 object_id；无法建立时显式 null，不推断默认值。"
        },
        "questions": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 questions；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "request",
        "target_id",
        "destination_id",
        "forbidden_ids",
        "clarification_required",
        "current_step_id",
        "object_id",
        "questions"
      ],
      "additionalProperties": true
    },
    "step": {
      "type": "object",
      "description": "规划器；依赖图与动作契约用于可执行性检查。",
      "properties": {
        "id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 id；无法建立时显式 null，不推断默认值。"
        },
        "action": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 action；无法建立时显式 null，不推断默认值。"
        },
        "arguments": {
          "type": [
            "object",
            "null"
          ],
          "description": "生产者声明的 arguments；无法建立时显式 null，不推断默认值。"
        },
        "state": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 state；无法建立时显式 null，不推断默认值。"
        },
        "depends_on": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 depends_on；无法建立时显式 null，不推断默认值。"
        },
        "expected": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 expected；无法建立时显式 null，不推断默认值。"
        },
        "deadline_ms": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 deadline_ms；无法建立时显式 null，不推断默认值。",
          "x-unit": "ms; UTC where a timestamp, duration where timeout; clock domain must be explicit"
        },
        "contract_ref": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 contract_ref；无法建立时显式 null，不推断默认值。"
        },
        "invariants": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 invariants；无法建立时显式 null，不推断默认值。"
        },
        "cancellation_policy": {
          "type": [
            "object",
            "null"
          ],
          "description": "生产者声明的 cancellation_policy；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "id",
        "action",
        "arguments",
        "state",
        "depends_on",
        "expected",
        "deadline_ms",
        "contract_ref",
        "invariants",
        "cancellation_policy"
      ],
      "additionalProperties": true
    },
    "tool_return": {
      "type": "object",
      "description": "工具执行器/执行账本；只声明派发及执行终态，不证明物理效果。",
      "properties": {
        "command_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 command_id；无法建立时显式 null，不推断默认值。"
        },
        "idempotency_key": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 idempotency_key；无法建立时显式 null，不推断默认值。"
        },
        "execution_state": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "NOT_STARTED",
            "STARTED",
            "SUCCEEDED",
            "FAILED",
            "UNKNOWN",
            null
          ],
          "description": "NOT_STARTED/STARTED/SUCCEEDED/FAILED/UNKNOWN；UNKNOWN 禁止未经对账重发。"
        },
        "started_ms": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 started_ms；无法建立时显式 null，不推断默认值。",
          "x-unit": "ms; UTC where a timestamp, duration where timeout; clock domain must be explicit"
        },
        "terminal_ms": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 terminal_ms；无法建立时显式 null，不推断默认值。",
          "x-unit": "ms; UTC where a timestamp, duration where timeout; clock domain must be explicit"
        },
        "error_code": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 error_code；无法建立时显式 null，不推断默认值。"
        },
        "acknowledgement_only": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "生产者声明的 acknowledgement_only；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "command_id",
        "idempotency_key",
        "execution_state",
        "started_ms",
        "terminal_ms",
        "error_code",
        "acknowledgement_only"
      ],
      "additionalProperties": true
    },
    "verification": {
      "type": "object",
      "description": "独立验证器；只转述有证据引用的验证结果。",
      "properties": {
        "report_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 report_id；无法建立时显式 null，不推断默认值。"
        },
        "verdict": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "VERIFIED",
            "FALSIFIED",
            "UNKNOWN",
            null
          ],
          "description": "VERIFIED/FALSIFIED/UNKNOWN；不从工具 SUCCEEDED 自动转换。"
        },
        "failure_type": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 failure_type；无法建立时显式 null，不推断默认值。"
        },
        "confidence": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0,
          "maximum": 1,
          "description": "声明者的置信度 [0,1]；不等于已校准的成功概率。"
        },
        "evidence_completeness": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0,
          "maximum": 1,
          "description": "证据完整度 [0,1]，不等于事实正确率。"
        },
        "verified_facts": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 verified_facts；无法建立时显式 null，不推断默认值。"
        },
        "falsified_facts": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 falsified_facts；无法建立时显式 null，不推断默认值。"
        },
        "unknowns": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 unknowns；无法建立时显式 null，不推断默认值。"
        },
        "verifier_version": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 verifier_version；无法建立时显式 null，不推断默认值。"
        },
        "contract_ref": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 contract_ref；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "report_id",
        "verdict",
        "failure_type",
        "confidence",
        "evidence_completeness",
        "verified_facts",
        "falsified_facts",
        "unknowns",
        "verifier_version",
        "contract_ref"
      ],
      "additionalProperties": true
    },
    "observation": {
      "type": "object",
      "description": "传感器适配器；保留单位、坐标系、校准与时钟域。",
      "properties": {
        "value": {
          "description": "原始或派生观测值；含数值、向量或对象，须由 unit/frame_id 和源 schema 解释。"
        },
        "unit": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 unit；无法建立时显式 null，不推断默认值。"
        },
        "frame_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 frame_id；无法建立时显式 null，不推断默认值。"
        },
        "covariance": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "array",
            "items": {
              "type": "number"
            }
          },
          "description": "与 value 同一坐标系和单位约定的协方差矩阵；null 不表示零噪声。"
        },
        "sensor_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 sensor_id；无法建立时显式 null，不推断默认值。"
        },
        "calibration_version": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 calibration_version；无法建立时显式 null，不推断默认值。"
        },
        "arrival_ms": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "接收端到达时间，不等于物理采样时间。",
          "x-unit": "ms; UTC where a timestamp, duration where timeout; clock domain must be explicit"
        },
        "edge_boot_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 edge_boot_id；无法建立时显式 null，不推断默认值。"
        },
        "edge_monotonic_ns": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "只在同一 edge_boot_id 内可比较，不能直接与 UTC 毫秒相减。",
          "x-unit": "ns; boot-local monotonic"
        },
        "clock_uncertainty_ms": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 clock_uncertainty_ms；无法建立时显式 null，不推断默认值。",
          "x-unit": "ms; UTC where a timestamp, duration where timeout; clock domain must be explicit"
        }
      },
      "required": [
        "value",
        "unit",
        "frame_id",
        "covariance",
        "sensor_id",
        "calibration_version",
        "arrival_ms",
        "edge_boot_id",
        "edge_monotonic_ns",
        "clock_uncertainty_ms"
      ],
      "additionalProperties": true
    },
    "guard": {
      "type": "object",
      "description": "权限与安全守卫；批准严格绑定具体动作参数与版本。",
      "properties": {
        "approved": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "生产者声明的 approved；无法建立时显式 null，不推断默认值。"
        },
        "approval_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 approval_id；无法建立时显式 null，不推断默认值。"
        },
        "approved_by": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 approved_by；无法建立时显式 null，不推断默认值。"
        },
        "approval_task_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 approval_task_id；无法建立时显式 null，不推断默认值。"
        },
        "approval_robot_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 approval_robot_id；无法建立时显式 null，不推断默认值。"
        },
        "approval_revision": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 approval_revision；无法建立时显式 null，不推断默认值。"
        },
        "approval_step_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 approval_step_id；无法建立时显式 null，不推断默认值。"
        },
        "approval_attempt_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 approval_attempt_id；无法建立时显式 null，不推断默认值。"
        },
        "approval_arguments_hash": {
          "type": [
            "string",
            "null"
          ],
          "description": "规范化实际执行参数的哈希；缺失时不能证明批准覆盖该动作。"
        },
        "expires_ms": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 expires_ms；无法建立时显式 null，不推断默认值。",
          "x-unit": "ms; UTC where a timestamp, duration where timeout; clock domain must be explicit"
        },
        "revoked": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "生产者声明的 revoked；无法建立时显式 null，不推断默认值。"
        },
        "budget_remaining": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 budget_remaining；无法建立时显式 null，不推断默认值。"
        },
        "automatic_retry_forbidden": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "生产者声明的 automatic_retry_forbidden；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "approved",
        "approval_id",
        "approved_by",
        "approval_task_id",
        "approval_robot_id",
        "approval_revision",
        "approval_step_id",
        "approval_attempt_id",
        "approval_arguments_hash",
        "expires_ms",
        "revoked",
        "budget_remaining",
        "automatic_retry_forbidden"
      ],
      "additionalProperties": true
    },
    "hypothesis": {
      "type": "object",
      "description": "Ops/Reflection/OptiAgent；假设和提议不具备执行权限。",
      "properties": {
        "claim": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 claim；无法建立时显式 null，不推断默认值。"
        },
        "supporting_evidence_ids": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 supporting_evidence_ids；无法建立时显式 null，不推断默认值。"
        },
        "contradicting_evidence_ids": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 contradicting_evidence_ids；无法建立时显式 null，不推断默认值。"
        },
        "confidence": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0,
          "maximum": 1,
          "description": "声明者的置信度 [0,1]；不等于已校准的成功概率。"
        },
        "proposed_test": {
          "type": [
            "object",
            "null"
          ],
          "description": "生产者声明的 proposed_test；无法建立时显式 null，不推断默认值。"
        },
        "status": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 status；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "claim",
        "supporting_evidence_ids",
        "contradicting_evidence_ids",
        "confidence",
        "proposed_test",
        "status"
      ],
      "additionalProperties": true
    },
    "attempt": {
      "type": "object",
      "description": "执行账本；未知终态必须查询，不得凭语言重复派发。",
      "properties": {
        "id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 id；无法建立时显式 null，不推断默认值。"
        },
        "tool": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 tool；无法建立时显式 null，不推断默认值。"
        },
        "arguments": {
          "type": [
            "object",
            "null"
          ],
          "description": "生产者声明的 arguments；无法建立时显式 null，不推断默认值。"
        },
        "arguments_hash": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 arguments_hash；无法建立时显式 null，不推断默认值。"
        },
        "idempotency_key": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 idempotency_key；无法建立时显式 null，不推断默认值。"
        },
        "execution_state": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "NOT_STARTED",
            "STARTED",
            "SUCCEEDED",
            "FAILED",
            "UNKNOWN",
            null
          ],
          "description": "NOT_STARTED/STARTED/SUCCEEDED/FAILED/UNKNOWN；UNKNOWN 禁止未经对账重发。"
        },
        "verdict": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "VERIFIED",
            "FALSIFIED",
            "UNKNOWN",
            null
          ],
          "description": "VERIFIED/FALSIFIED/UNKNOWN；不从工具 SUCCEEDED 自动转换。"
        },
        "evidence_ids": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 evidence_ids；无法建立时显式 null，不推断默认值。"
        },
        "failure_code": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 failure_code；无法建立时显式 null，不推断默认值。"
        },
        "progress_measure": {
          "type": [
            "object",
            "null"
          ],
          "description": "生产者声明的 progress_measure；无法建立时显式 null，不推断默认值。"
        },
        "approval_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 approval_id；无法建立时显式 null，不推断默认值。"
        },
        "compensation_ref": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 compensation_ref；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "id",
        "tool",
        "arguments",
        "arguments_hash",
        "idempotency_key",
        "execution_state",
        "verdict",
        "evidence_ids",
        "failure_code",
        "progress_measure",
        "approval_id",
        "compensation_ref"
      ],
      "additionalProperties": true
    },
    "tool": {
      "type": "object",
      "description": "工具注册表；版本和参数 schema 应与实际执行端一致。",
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 name；无法建立时显式 null，不推断默认值。"
        },
        "description": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 description；无法建立时显式 null，不推断默认值。"
        },
        "argument_schema": {
          "type": [
            "object",
            "null"
          ],
          "description": "生产者声明的 argument_schema；无法建立时显式 null，不推断默认值。"
        },
        "mutates_world": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "生产者声明的 mutates_world；无法建立时显式 null，不推断默认值。"
        },
        "requires_approval": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "执行端仍独立校验批准，不由语言输出授予。"
        },
        "preconditions": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 preconditions；无法建立时显式 null，不推断默认值。"
        },
        "postconditions": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 postconditions；无法建立时显式 null，不推断默认值。"
        },
        "timeout_ms": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 timeout_ms；无法建立时显式 null，不推断默认值。",
          "x-unit": "ms; UTC where a timestamp, duration where timeout; clock domain must be explicit"
        },
        "idempotency": {
          "type": [
            "object",
            "null"
          ],
          "description": "生产者声明的 idempotency；无法建立时显式 null，不推断默认值。"
        },
        "read_only": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "生产者声明的 read_only；无法建立时显式 null，不推断默认值。"
        },
        "tool_version": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 tool_version；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "name",
        "description",
        "argument_schema",
        "mutates_world",
        "requires_approval",
        "preconditions",
        "postconditions",
        "timeout_ms",
        "idempotency",
        "read_only",
        "tool_version"
      ],
      "additionalProperties": true
    },
    "handoff": {
      "type": "object",
      "description": "编排器与持久化账本；恢复前必须处理未决命令。",
      "properties": {
        "ledger_sequence": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 ledger_sequence；无法建立时显式 null，不推断默认值。"
        },
        "history_complete": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "相对于声明的账本范围；false 必须保留缺口。"
        },
        "omitted_events": {
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0,
          "description": "生产者声明的 omitted_events；无法建立时显式 null，不推断默认值。"
        },
        "unresolved_attempt_ids": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 unresolved_attempt_ids；无法建立时显式 null，不推断默认值。"
        },
        "cancellation_state": {
          "type": [
            "string",
            "null"
          ],
          "description": "生产者声明的 cancellation_state；无法建立时显式 null，不推断默认值。"
        },
        "resource_leases": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "object"
          },
          "description": "生产者声明的 resource_leases；无法建立时显式 null，不推断默认值。"
        },
        "resume_preconditions": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "生产者声明的 resume_preconditions；无法建立时显式 null，不推断默认值。"
        }
      },
      "required": [
        "ledger_sequence",
        "history_complete",
        "omitted_events",
        "unresolved_attempt_ids",
        "cancellation_state",
        "resource_leases",
        "resume_preconditions"
      ],
      "additionalProperties": true
    }
  }
}
