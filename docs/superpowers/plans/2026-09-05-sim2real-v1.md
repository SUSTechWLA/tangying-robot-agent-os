# Sim2Real V1 integration and handover

Goal: give an XLeRobot customer a concrete, inspectable route from simulation to a supervised physical pilot, and make the repository understandable to a new developer.

Architecture: single-primary Fleet, durable world checkpoint, laptop Edge/policy, Raspberry Pi direct runtime with explicit torque enable, device-specific commissioning package. Hardware validation remains a separate measured release gate.

Technology: existing Go services, Python runtime and policy contracts, systemd/Compose, Markdown documentation. Preserve current working changes and the completed operator/developer UI.

- [x] Add a no-motion commissioning CLI with generated configuration, artifact/revision checks, immutable operator evidence, and a readable report.
- [x] Separate driver connection from explicit arm enable; adapt the pinned upstream defects with regression tests.
- [x] Harden the perception/verification boundary and runtime startup/identity handling.
- [x] Persist authoritative world checkpoints and enable a writable volume in the production Compose profile.
- [x] Write customer Sim2Real guide and V1 release status; refresh active installation, architecture, API, frontend and developer documents.
- [x] Run targeted regressions, Go/Python/Web checks and verify documentation links. Record actual results and untested hardware/deployment boundaries.

Acceptance: setup never moves hardware; missing/mismatched configuration has actionable blockers; stale or altered evidence is rejected; stop/reset cannot silently enable torque; recovered world cannot masquerade as fresh; documentation does not call an offline pass hardware readiness.

Verification: Python full 710 passed / 33 skipped; final changed packages 111 passed; Go 43 tested packages; selected race 21 packages; Web127; actual isolated MySQL8.4.11 migration5; docs contracts22. Source changes are not a signed release or physical commissioning certificate. Logs: artifacts/acceptance/sim2real-v1-2026-09-05/.
