# V1 当前交付状态

更新：2026-09-08（文档与当前源码同步）。本文描述本工作区升级后的能力，替代早期审计中的“当前状态”判断；历史证据包继续保留其原始版本和时间。当前改动尚未生成新的签名发布包，也未部署到客户实机。

**本轮收敛到单机器人 RGB-D 闭环，增加可恢复任务与持久化相机证据，并完成彩色点云可读性修复。** 当前验证结果和实机交付边界见[单机器人 V1](single-robot-v1.md)；下方较早记录保留其原始数字，不自动代表最新源码。

## 可以交付到什么程度

**当前定位是可开展现场集成与验收的单主、受监护实机试点候选版。** 仿真闭环、系统接口与软件防护已实现；现成实机感知、通用抓取模型、现场安全与长期可靠性不能由代码测试代替。不能宣布“购买 XLeRobot 后直接插上即可无人值守生产”。

第一版现场范围：一个主控制平面、限定工位/物体/桌面任务、XLeRobot 双臂和头部、底盘禁用、现场人员负责物理急停与班次使能。平台现在允许新型号通过严格 profile 和规范观测接入，支持不同关节、末端和传感器；每个实机适配器仍须独立开发驱动、标定、策略和验收。导航 Runtime 接口不代表已经具备通用导航规划。多主 HA、任意环境家务、自主移动和无人值守运行不在本版放行范围。

客户操作入口：[购机后 Sim2Real 上手](../sim2real/README.md)。新开发入口：[开发快速上手](../development/getting-started.md)、[异构机器人适配器](../development/robot-adapters.md)。

## 功能与证据对齐

| 项目 | 当前实现 | 验证边界/交付要求 |
| --- | --- | --- |
| 仿真全流程 | 自然语言/确定性规划、版本确认、策略动作、观测、Harness 裁决、失败恢复与审计 | RoboCasa 已验证单回合定向交接；反向搬运、完成后通用重新授权未实现 |
| 自然语言 | 完整已知请求优先确定性解析，支持中文编号、口语别称、同句代词、独立起点/终点；已识别的否定/条件/歧义要求澄清 | 固定 13 项符合预期，不是开放域理解准确率；无跨任务指代、任意条件工作流或通用家务能力 |
| 执行前检查 | 物体/终点唯一匹配；指定起点时确认唯一来源及 inside/on 关系；终点修改不丢弃额外约束 | 场景方向与回合授权尚未完全前置到创建/审批预检，仍可能在 Runtime 拒绝 |
| 前端 | 明亮中文工作台、Local RGB/depth/局部点云及历史证据，用户/开发模式；Fleet 旧四场景扩展保留 | 纯用户 edition 仅界面裁剪，不是服务端权限隔离；当前 UI/资产需独立签名采集 |
| 按设备接入 | `robot-agent sim2real init/check/record/report`，私有配置包、模型/标定/身份核对、测试附件摘要、版本失效检查 | 离线资料门槛；不能验证实机运行的是所填构建，也不证明人工证据真实 |
| 驱动兼容 | 锁定 XLeRobot 源码与 LeRobot 版本；修正双总线配置、关节键及连接/扭矩行为 | 执行实际锁定类 AST + 假总线回归；真实串口、扭矩与时序待实测 |
| 异构接入 | `robot.profile.v1` 描述结构、关节、末端、来源、动作范围及规范工具；可信本地 factory 复用 Runtime | 两种内存示例及真实 Go/Python 七步链已验证；物料 grant 仍需可信宿主同步，不是 Fleet custody 全链路验收；旧适配器不自动获得严格认证 |
| 统一 MCP | 官方 SDK stdio 桥接设备、能力、世界、任务与停止 API；8 个工具共用 Fleet 身份与任务状态 | 创建保留人工审批；不提供原始动作、解锁、批准或复位工具；未知请求结果不自动重试 |
| 连接与使能 | 服务默认连接但不 arm；现场交互命令显式使能机械臂/头部；底盘不使能 | 连接也会禁用扭矩、写寄存器，需支撑机械臂；不是无物理影响操作 |
| 停止与恢复 | 停止撤销使能；持久急停；独立本地复位、原始 journal 审计、不确定命令永久禁止重放 | 需要现场确认；未配置实机回位轨迹，`recover_to_safe_pose` 标为不可用；腐坏 journal 拒绝自动清除 |
| Runtime 安全 | 执行前日志、并发去重、未知结果保护、取消/租约/急停竞态防护、动作键与有限值验证 | 软件停止没有硬实时证明，必须物理测试 |
| 真实感知 | 新 profile 必须提供 `scene.reconstruction.v1`；Python/Go 校验世界坐标、SI 单位、来源/标定、采集时刻、递增帧与有界点集，保留语义关系 | 已提供 RGB-D 反投影、有限工位检测器和局部点云前端；未提供通用视觉、稠密地图或 SLAM。真实相机/ROS transport、控制器和现场精度待实测；旧 provider 不自动成为严格输入 |
| 真实策略 | HTTP sidecar 与版本化 manifest，实际权重哈希、动作/观测约束、拒绝实机 deterministic | 需要用户工位适用的模型、训练数据和延迟评估；LLM 不代替动作模型 |
| 物理结果验证 | verifier 接口及返回值/置信度校验 | 真实动作后成功判断由集成模块与现场证据完成，不能固定返回成功 |
| World 恢复 | opt-in 文件检查点，原子写与 fsync，本地独占锁；恢复版本/去重信息，恢复内容陈旧，新鲜事实重新累计 | 单主完整快照；跨主机 HA、业务事务 fencing 未由此解决；长期容量/吞吐待测 |
| 生产 Compose | 持久 `fleet-world` 卷、非 root 用户写权限；不公开控制面原始端口 | 新卷权限已用 Docker 实测；客户域名、证书、备份、磁盘与实际 MySQL 迁移需部署验收 |
| 权限与配置 | operator/device 身份、设备独立凭据、mTLS/HTTPS | 当前没有细粒度五角色 RBAC；纯用户发行需后端授权验收 |
| 文档 | 用户接入、开发入门、源码原理、安装、接口/契约、部署运维、前端与证据索引更新 | 历史设计/计划保留并标为历史，不把旧数字或旧命令当现版依据 |

## 本次验证记录

### 2026-09-08 彩色点云与桌面小物体修复

旧均匀抽样把大量预算用于地面和背景，且前端统一单色、固定画布字号，导致杯和瓶难以辨认。现在每个采样点绑定同一原始像素的 XYZ/RGB，给检测物体保留测量点；参考工位从 2048 点提高至合同上限 4096 点。前端按实测工作区取景，点和标签按 CSS 尺寸显示，名称优先任务物品；没有颜色的旧记录明确单色，非法颜色不显示。

- 同一个参考工位采集的独立对比：旧均匀 2048 点中，红杯有效彩色表面点 **25**、蓝瓶 **22**；新采样分别 **418**、**416**。全部 **4096/4096** 个点反投影回 RGB 后颜色完全一致，深度数值往返误差小于 `1e-12 m`。这是同一模拟帧的算法一致性，不是实机标定精度。
- 浏览器任务 `task-76db3889947ebfee4b412b05` 完成“把红色杯子放进右侧收纳盒，然后把蓝色瓶子拿过来”。最终相机关系为 `inside:right-bin` 与 `inside:front-tray`；**16 次历史采集**均保存 4096 个点和 4096 个对应 RGB，最终历史图片 SHA-256 核对通过。浏览器已核对彩色点云、可读名称和同帧历史图片。
- 最终 `make test` 退出 0：**44 个 Go 测试包**、隔离 Runtime 边界 **2 passed**、Python 主集 **915 passed / 33 skipped**（264.68s）、前端 **176 passed**。33 项仍为主环境缺少 RoboCasa 依赖，2 条既有 pytest 模块重写警告，不记为实机验收。
- 合同、机器人客户端、任务、SQLite 和 Console 五包 Go race 通过；`make lint` 和 `git diff --check` 通过。旧无色、错位颜色、非法通道、直接 JSON 的 `null` 通道、同帧改色、深复制与持久化均有回归。

复现 RGB/XYZ 对齐和物体保留：运行 `.venv/bin/pytest -q robot/gateway/tests/test_rgbd.py sim/mujoco/tests/test_rgbd_runtime.py`；浏览器启动用 `make rgbd-start`。本机原始报告、彩色帧和测试日志位于忽略目录 `artifacts/acceptance/rgbd-colour-2026-09-08/`，不作为随仓库分发的签名包。颜色来自已测像素，没有新增未观测几何、SLAM 或实机验收结论。

### 2026-09-08 单机器人 RGB-D 与可恢复任务（前一轮）

本轮以 Local 单机器人为首版主线，保留 Fleet、多机器人身份和规范工具扩展。新增通用 RGB-D 帧/反投影、机载相机参考工位检测、同帧图像与局部点云、动作后视觉关系验证。完整模拟世界仍可供开发检查；它不是这条路线的环境输入。随附检测器只覆盖红杯、蓝瓶与三个容器，控制器仍有理想抓取附着及用于参考运动的底盘 IK，不是可直接迁移的实机抓取策略。

实际自然语言及中断验收：

| 场景 | 结果 |
| --- | --- |
| 红杯入右盒，再把蓝瓶拿过来 | 14 工具分解；RGB-D 最终关系为 `red-cup → right-bin`、`blue-bottle → front-tray` |
| 拿起杯子后暂停，只重启 Agent | 同 task/revision 继续成功，抓取/放置各 2 次，无物理步骤重复；20 条持久观测、17 个关联 capture |
| 抓取中直接终止 Agent | 重启后恢复返回 HTTP 409 `PHYSICAL_OUTCOME_UNKNOWN`，无第二次抓取或放置 |
| 相机中没有绿色杯子 | 目标绑定失败，0 次物理动作 |
| 历史回看 | 按目标/工具选择原始采集，真实同帧 RGB/depth 与哈希一致；时间明确标记历史 |

命令和任务 ID 见[单机器人 V1](single-robot-v1.md#验收记录)。`scripts/run_rgbd_acceptance.py` 在独立端口创建自身 Runtime/Agent/SQLite，保留 Runtime 现场完成 Agent 重启试验；两种恢复路径已加入 `tests/e2e/test_rgbd_recovery.py`。

- 最终 `make test` 退出 0：Go 44 个有测试包通过；隔离 Runtime 契约 **2 passed**；Python 主集 **887 passed、33 skipped**（267.23s，2 条既有模块重写警告）；Web **165 passed**；其后两项中文失败原因/Local 完成文案修正的最终 `make test-web` 为 **167 passed**。33 项跳过为主环境缺少 RoboCasa 依赖，本轮未将上一轮独立环境结果记为本轮复验。
- 七包 `go test -race` 通过：`edge/agent`、`internal/localapp`、`console`、`tasks`、`middleware/sqlite`、`edge/robotclient`、`edge/worker`；另 `cmd/local-agent` 的 race 与配置测试通过。
- `make lint`、生成源文件重新生成前后字节一致性、差异格式检查通过。更早整体验证只有一个旧测试仍期待 `AWAITING_EVIDENCE`，现按已持久完成语义改为 `CONFIRMED`，定向重跑和最终完整回归均通过，未修改动作成功条件。
- 独立审查修复渲染旧帧借用新时间戳、识别耗时耗尽新鲜度、只读失败被误判未知物理动作、证据写入与任务事务短暂锁竞争等问题。旧/未来/空深度、伪造持物、恢复期间物体丢失、目标变更、跨任务证据及原始内容过期均有回归。
- 浏览器实际验证彩色/深度/点云、历史任务重新选择、原只读失败任务继续成功和历史双图回看。点云仅显示实际点，缺失保持未知；任务完成后清除过时故障建议。

原始日志与帧位于本机忽略目录 `artifacts/acceptance/rgbd-v1-2026-09-08/`；克隆项目后按脚本重新采集，不把本地目录当随仓库发布的签名证据包。本轮没有运行真实 ROS/DDS、真实 RGB-D 相机、真实电机或现场安全试验。ROS 输入桥通过消息/变换合同测试，默认只观察；完整 SLAM、开放域视觉、通用接触抓取和无人值守生产不属于已验证能力。

### 2026-09-07 异构接入与回归修复（9 月 6 日开始）

本轮增加 profile/规范三维观测的 Python 与 Go 校验、插件生命周期、通用工具和 MCP；从真实 Go 规划器生成七步任务，通过 gRPC 驱动明确标记的示例仿真，确认最终放置关系与持物状态。SDK 示例没有验证真实 RGB-D、LiDAR 或导航算法。

上一轮三项交接失败根因是 MuJoCo 已有物理放置但未发布起点绑定需要的 inside 关系；现在按当前几何和持有状态输出关系，并在物体移开时撤销旧关系，未放松起点或 Harness 检查。另修复历史签名验收误依赖当前前端版本：历史包使用已经签名验证的资源清单，新候选与基线提升仍要求当前源码匹配。

本轮还修复本地演示在 Runtime 未监听时提前审批的竞态；先等待 Runtime 及新鲜场景，执行恢复失败及时退出并清理自身进程。测试栈改为等完整 FRESH 投影，渲染锁回归改为等待线程实际完成；未修改生产 freshness 预算或世界锁。

- SDK 与真实 Go/Python 边界定向验证 **200 passed**；MCP（含真实 stdio、私有 CA/主机名拒绝）**22 passed**；演示延迟启动/首帧/恢复失败及清理 **7 passed**，独立 `bash scripts/demo.sh` 为 `SUCCEEDED`、29 个事件。
- `go test -race ./core/robotcontract ./edge/robotclient ./edge/worker ./cmd/edge-worker` 通过；生成代码一致性与 lint 通过。
- 主环境的 33 项 RoboCasa 缺依赖跳过已独立补测：现有 `tangying-robocasa` 环境单元测试 **46 passed**（覆盖其中 27 项）；对应 6 项真实流程首轮 5 passed、1 项因 10 秒解释器探针超时跳过，该项原样单独重跑 **1 passed**。未升级依赖；该环境未安装 pytest-timeout，保留配置警告，流程本身另有分阶段截止时间。
- 最终 `make test` **退出 0**：Go **44 个有测试的包通过**；隔离 Runtime 边界 **2 passed**；Python 主集 **826 passed、33 skipped**（718.16s，2 条既有 pytest 模块重写警告）；Web **137 passed、0 failed**。33 项跳过均为 RoboCasa 环境依赖，已按上一条在独立环境完成补测。此前失败的三项交接以及本轮发现的世界就绪、demo 启动与渲染同步用例均在此完整回归中通过。
- 更新文档 18 页、180 个本地链接/锚点、94 处 Make 引用检查通过；暂存差异格式检查通过，截图、私钥、运行 journal 和本地测试日志不进入提交。

本轮日志位于本地忽略目录 `artifacts/acceptance/heterogeneous-robots-2026-09-06/`；未重新生成签名发布包、连接实机或部署客户环境。

### 上一轮 2026-09-06 提交前验证（历史失败，已在本轮修复）

- `make test` 的 Go 测试及 Go/Python Runtime 边界 2 项通过；Python 主测试集为 **708 passed、3 failed、33 skipped**，命令最终退出失败。
- 三个失败用例：`tests/e2e/test_fleet_faults.py::test_edge_disconnect_reconnect_completes_without_false_advance`、`tests/e2e/test_fleet_handoff.py::test_natural_language_shared_block_handoff_is_world_verified`、`tests/e2e/test_policy_handoff.py::test_natural_language_handoff_records_policy_observation_and_harness_evidence`。当时三者均以任务 `FAILED` 结束；断连恢复用例的事件明确显示 robot-2 起点绑定拒绝：未观察到 red-block 位于 handoff-zone 的 inside/on 关系。本轮在 MuJoCo 观测源修复并重新执行这三项，保留本段原始失败记录。
- 单独执行 `make test-web`：137 项通过；`make lint` 通过。暂存差异格式检查通过，但两份保留原始字节的上游驱动测试夹具带有上游尾随空白，检查时明确排除该两份文件。
- 原始日志保存在本地忽略目录 `artifacts/acceptance/pre-push-2026-09-06/`，不随 Git 推送。远端 `codex/v0.1` 已有另一路 15 个提交，合并预检有 19 个文件冲突；本轮候选代码发布到独立 `codex/v0.1-production-upgrade` 分支，尚未合入那些远端更新。

### 2026-09-05 自然语言与前端后续修复

完整结果与复现见[自然语言评测](../development/natural-language-evaluation.md)。本机原始报告位于 `artifacts/natural-language-eval/final/`；该忽略目录不随 Git 克隆交付。这些日志不是签名生产证据包。

- 固定 13 项符合预期：5 条正向任务 `SUCCEEDED` 且 Harness/新鲜世界证据齐全，6 条创建时返回 422，2 条因物体缺失或起点不符执行前失败且物体未移动。
- 相同的前 12 项从 3/12 改进到 12/12；额外反向搬运探索失败（`FENCING_TOKEN_STALE`），独立记录，未计入 13 项通过率。
- `make test-go` 项目 Go 包通过；任务更新测试 3 项通过，其中一项启动真实 RoboCasa 进程，验证保留已完成步骤并等待安全点切换。
- 前端最终 `make test-web` 为 137 项通过，后续 `go test ./web/...`、文档契约 5 项、评测脚本 Ruff 与差异检查通过。
- 浏览器实际执行过含“把它”的双机任务并成功；修复后确认两步均显示完成及环境证据，三维实时同步。此补测实际调用仿真，与早期拦截任务写请求的布局测试不同。

使用确定性语义仿真策略，没有评测线上大模型、重新训练权重或验证真实机械臂。文档同步阶段没有重跑全量 Python；随后提交前全量测试的最新结果见上方记录，未进行实机或签名采集。

### 2026-09-06 文档对齐验证

核对当前 Parser、起点绑定、Revision/Experience、WS 代理与 RoboCasa 回合实现，并重读原始语言评测和浏览器补测报告。检查 40 份当前文档与入口文件、295 个本地链接/锚点、131 处 Make 引用、6 个 JSON 示例，未发现错误；文档/README/仓库契约共 22 项通过，前端 137 项复测通过，差异格式检查通过。记录位于 `artifacts/acceptance/docs-sync-2026-09-06/`。历史文档未改写，本次没有重新执行机器人任务或部署服务。

### 此前的 V1 加固验证（保留原始范围）

此前输出位于 `artifacts/acceptance/sim2real-v1-2026-09-05/`。以下是自然语言/前端后续修改前的分阶段结果，不是最新前端测试数量，也不表示每次改动都重跑了全部测试。

- Python 最终全量回归 710 passed、33 skipped；随后对末次修改（策略接入检查、实机恢复能力描述等）的相关包再次验证，111 passed。初轮发现的 7 处旧文档/脚本契约失败已修复。33 个跳过项不计为通过；未进行实机测试。
- Go 全量 43 个有测试的包通过，core/fleet/CLI 相关 21 个包的 race 检查通过；Web 127 项测试通过；Go 四个可执行文件构建及全仓 lint 通过。
- 独立 MySQL 8.4.11 临时容器中，`go test -race -count=1 -v ./fleet/mysql` 的 5 项测试通过，包括新库/旧库迁移、重复启动和旧数据保留；容器与匿名卷已清理，未接触现有云端数据。
- 当前 active 文档 38 页、247 个本地链接/锚点、Make 目标和 6 个 JSON 示例检查通过；README/仓库/文档合同 22 项通过。
- 驱动/启动/校准与 Runtime 合同的定向验证通过；含加载锁定上游源代码、双总线故障、信号中断使能时的清理与禁止自动 arm。
- WorldHub 的 core/fleet race 验证通过；含真实子进程无清理退出后恢复、损坏文件、并发写、保存失败和陈旧观测。
- 接入包测试通过；含模型/身份不匹配、证据篡改、旧版本失效、重复任务、提前失败的连续观察及标定/变换文件哈希绑定。
- Docker 新命名卷以 `nobody:nogroup`（65534:65534）和 0700 权限成功写入。旧的非空卷不会自动修复权限，升级前需运维核对。

Python 最终全量与最后修改包的结果见本次验证日志。未连接真实 XLeRobot，未以当前改动完成客户云部署，未重新采集签名 round4。

## 现场上线前必须补齐

1. 核对到货硬件版本、线束、电机 ID、物理急停和工位负载；确认锁定驱动适配。
2. 获得电机/相机标定、真实感知、动作策略及动作后验证。绑定权重、标定、坐标、任务范围与交付构建。
3. 在现场测量急停/断网/进程故障的停止结果、持物风险、恢复行为和成功率；保存失败及至少 30 次不同实机任务与连续观察记录。
4. 验证实际 MySQL 迁移、备份恢复、证书续期、磁盘容量、日志保留与故障值班；测量 World 快照长期写入成本。
5. 审阅当前构建证据，形成新的候选包与签字放行记录；再交付普通用户使用。

已知后续功能包括场景感知澄清、跨任务上下文、可验证的回合重置/重新授权和反向搬运、旧 XLeRobot provider 迁移到严格感知合同、真实传感器与新型号驱动、可信物料授权同步与 Runtime 独立 revision/step 校验、导航技能与稠密地图、图形化标定和模型导入、纯用户端服务端权限裁剪、更长时间压力测试，以及跨主机高可用。它们不阻止限定现场集成，但阻止把候选版标为通用无人值守成品。
