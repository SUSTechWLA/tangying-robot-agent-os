# Private Docker Compose interpolation file on the Orin NX host.
# Create a dedicated host group allowed to read only agent env and TLS files;
# replace this example GID with its numeric ID before running Compose.
EDGE_CERT_GID=12345
AGENT_IMAGE_TAG=dev
