CLOUD_BIND=127.0.0.1
CLOUD_PORT=8080
POSTGRES_BIND=127.0.0.1
POSTGRES_PORT=54329
POSTGRES_DB=robot_agent
POSTGRES_USER=robot_agent
POSTGRES_PASSWORD=replace-before-network-exposure
# Optional OpenAI-compatible task agent and LLM orchestrator.
# Deterministic parser and planner remain the fallback.
AGENT_PROVIDER=deterministic
# AGENT_PROVIDER=openai
# AGENT_BASE_URL=https://your-provider.example/v1
# AGENT_API_KEY=
# AGENT_MODEL=
# AGENT_ORCHESTRATION_SAMPLES=1

